修复 Windows 发布包中的 FFmpeg 路径问题。

- 构建流程现在拒绝 Chocolatey shim，只打包真实的 ffmpeg.exe 和 ffprobe.exe
- 修复另一台电脑导入视频时提示“无法读取媒体文件”
- 保留静态 latest.json 更新检查，不依赖 GitHub API 配额

从 v1.0.1 升级时请手动安装一次 v1.0.2 兼容安装包。后续版本可直接使用程序内“检查更新”。
