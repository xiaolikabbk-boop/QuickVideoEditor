修复更新检查可靠性。

- 更新源改为 GitHub Release 静态 `latest.json`
- 不再依赖 GitHub 匿名 API 配额，避免 `403 rate limit exceeded`
- 保留旧 Release 的 API 兼容回退
- 增加不启动临时 `Setup.tmp` 的兼容安装包

从 `v1.0.0` 升级时，请先手动安装一次 `v1.0.1` 兼容安装包。此后的版本可以直接使用程序内“检查更新”。
